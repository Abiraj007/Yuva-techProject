# Yuva-techProject
This Java program generates an N × N matrix filled with random even numbers, prints it, allows the user to choose a number to evict, and then highlights all occurrences of that number inside the matrix.

Features 📲:
Takes input N from user (size of the matrix).
Creates an N × N grid.
Fills each cell with a random even number chosen from a predefined list.
Prints the generated matrix.
Asks the user to enter a number to evict.
Replaces every matching value with [value] to highlight it.
Counts and displays how many times the number appeared.
Prints the updated matrix after eviction.
